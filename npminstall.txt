sudo npm install -g winston pkginfo watch mersenne locale async serializer redis expiring-lru-cache sprintf optimist colors request redis-lock node-iplookup mongodb zmq should express range-parser fresh

npm install winston pkginfo watch mersenne locale async serializer redis expiring-lru-cache sprintf optimist colors request redis-lock node-iplookup mongodb zmq should express range-parser fresh

sudo npm install -g mocha

npm install git+https://github.com/jacobbubu/WebSocket-Node.git

安装tern程序
git clone https://github.com/jacobbubu/tern_services.git